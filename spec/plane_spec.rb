require 'plane'

describe Plane do

  # Unit test to check correct instantiation of Plane object
  it 'when instantiated it creates an instance of Plane class' do
    expect(subject).to be_an_instance_of(Plane)
  end

end
