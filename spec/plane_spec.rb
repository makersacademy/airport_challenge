require 'plane'

describe Plane do 
	
#	it "responds to land" do
#		expect(subject).to respond_to(:land).with(1).argument
#	end
#
#	it "checks whether it has landed or not" do
#		subject.land(Airport.new)
#		expect(subject.landed?).to eq true
#	end
#
end