require "airport"

describe Plane do
  # describe "#flying?" do
  #   it "planes that are not flying cannot land" do
  #     airport = Airport.new
  #     allow(subject).to receive(:flying?) { false }
  #     expect { airport.land(subject) }.to raise_error("This plane has already landed")
  #   end
  # end
end
