require 'traffic_controller'

describe Controller do
  describe 'test that class is being created' do
    it 'can create an instance of the class' do
      expect(subject).to be_a(Controller)
    end
  end
end
