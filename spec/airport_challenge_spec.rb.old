require 'weather'
require 'plane'

describe controller do

# Arrange
  it "must create a weather status" do
    controller = Controller.new
  end

  it "must stop take off if weather status is stormy " do
    weather = Weather.new
    weather.set_stormy
    expect(weather.stormy?).to eq true
    
  end


end

