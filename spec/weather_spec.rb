# require 'weather'
#
# describe Weather do
#   it 'tests weather and returns false for stormy' do
#     weather = Weather.new
#     allow(weather).to receive(:rand).and_return(3)
#     expect(weather.stormy?).to eq(false)
#   end
#
#   it 'tests weather and returns true for stormy' do
#     weather = Weather.new
#     allow(weather).to receive(:rand).and_return(1)
#     expect(weather.stormy?).to eq(true)
#   end
#
#
# end
