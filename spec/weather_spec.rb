require 'weather'

describe Weather do

  # it "responds to a method is_stormy" do
  #   expect(subject).to respond_to (:is_stormy?)
  # end
  #
  # it "return_random_number should return a random number" do
  #   random_number = subject.return_random_number
  #   expect(subject.return_random_number).not_to eq random_number
  # end
  #
  # it "return_random should be between 1 and 100" do
  #   expect(subject.return_random_number).to be_between(1,100).inclusive
  # end

end
