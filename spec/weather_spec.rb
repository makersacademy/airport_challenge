# require 'weather'
#
# describe Weather do
#   it { is_expected.to respond_to :condition }
#   it { is_expected.to respond_to(:safe?).with(1).argument }
#
#   it 'is safe to flight' do
#     expect(subject.safe?('yes')).to eq 'safe'
#   end
#
#   it 'is safe to flight' do
#     expect(subject.safe?('no')).to eq 'not safe'
#   end
#
# end
